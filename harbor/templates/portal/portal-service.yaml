apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: harborlast
  labels:
    app: portal
spec:
  type: LoadBalancer  # Keep LoadBalancer for external access
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: portal
