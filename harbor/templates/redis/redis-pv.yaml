apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
  labels:
    app: harbor
    component: redis
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: gp2
  hostPath:
    path: /mnt/data/redis
